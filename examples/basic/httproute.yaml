apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: echo
spec:
  parentRefs:
    - name: cloudflare-tunnel
      namespace: cfgate-system
  hostnames:
    - echo.example.com  # Replace with your domain
  rules:
    - backendRefs:
        - name: echo
          port: 80
