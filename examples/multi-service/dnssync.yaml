apiVersion: cfgate.io/v1alpha1
kind: CloudflareDNSSync
metadata:
  name: multi-dns
  namespace: cfgate-system
spec:
  tunnelRef:
    name: multi-tunnel
  zones:
    - name: example.com
  source:
    gatewayRoutes:
      enabled: true
  defaults:
    proxied: true
